<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!--
      Lets this app ask the LocalLLM app to write a summary of analytics this app has
      already received. The permission is signature|knownSigner and is granted because this
      app's signing certificate is listed in LocalLLM's known_signers.xml; the two apps do
      not share a key. LocalLLM must be installed first. Absence is handled gracefully: the
      summary card simply does not appear.
    -->
    <uses-permission android:name="com.noamv.localllm.permission.INFERENCE" />

    <!-- Required from Android 11 for this app to see the LocalLLM service at all. -->
    <queries>
        <package android:name="com.noamv.localllm" />
    </queries>


    <application
        android:name=".CannsheetApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>
        <activity
            android:name=".widget.PenWidgetConfigureActivity"
            android:exported="true"
            android:label="@string/pen_widget_configure_label"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>
        <receiver
            android:name=".widget.PenConsumptionWidgetProvider"
            android:exported="false"
            android:label="@string/pen_widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/pen_consumption_widget_info" />
        </receiver>
        <receiver
            android:name=".widget.sync.SyncStatusWidgetProvider"
            android:exported="false"
            android:label="@string/sync_status_widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/sync_status_widget_info" />
        </receiver>
        <receiver
            android:name=".widget.multi.MultiCartWidgetProvider"
            android:exported="false"
            android:label="@string/multi_cart_widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/multi_cart_widget_info" />
        </receiver>
        <service
            android:name=".widget.PenQuickTileService"
            android:exported="true"
            android:icon="@drawable/ic_tile_pen"
            android:label="@string/pen_tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>
    </application>

</manifest>
